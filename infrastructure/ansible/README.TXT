Target: Debian 8.3 (jessie)

Scripts:
  cs-requirements.sh  - install requirements for checksystem (run first)
  cs-install.sh       - install checksystem itself (run second, git repo with checksystem should exist)

